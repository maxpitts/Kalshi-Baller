# ═══════════════════════════════════════════════════════════════
# YOLO ENGINE — Configuration
# ═══════════════════════════════════════════════════════════════

# Kalshi API credentials
KALSHI_API_KEY_ID=your-api-key-id-here
KALSHI_PRIVATE_KEY_PATH=./kalshi-key.pem

# OR use base64-encoded key (for Railway / cloud deploys)
# Generate with: cat kalshi-key.pem | base64
# KALSHI_PRIVATE_KEY_BASE64=LS0tLS1CRUdJTi...

# Environment: "production" or "demo"
KALSHI_ENV=demo

# ═══════════════════════════════════════════════════════════════
# STRATEGY CONFIG
# ═══════════════════════════════════════════════════════════════

# Starting bankroll in dollars
STARTING_BANKROLL=100

# Target in dollars
TARGET_BANKROLL=10000

# Time limit in hours before self-destruct
TIME_LIMIT_HOURS=48

# Strategy mode: "edge_hunter" | "momentum" | "full_send"
# edge_hunter  = Find mispriced contracts, Kelly-adjacent sizing
# momentum     = Ride contracts with volume spikes
# full_send    = All-in on best opportunity each round
STRATEGY_MODE=edge_hunter

# Max percentage of bankroll per single bet (0.0 - 1.0)
MAX_BET_FRACTION=0.5

# Minimum edge required before placing bet (0.0 - 1.0)
# Higher = more selective, fewer bets
MIN_EDGE_THRESHOLD=0.05

# Scan interval in seconds
SCAN_INTERVAL_SECONDS=30

# ═══════════════════════════════════════════════════════════════
# SELF-DESTRUCT CONFIG
# ═══════════════════════════════════════════════════════════════

# What happens when time runs out or bankroll hits zero
# "withdraw"  = Cancel all orders, close positions, withdraw remaining
# "halt"      = Just stop the bot, leave positions open
# "nuke"      = Cancel everything and delete config files
DESTRUCT_MODE=withdraw

# Dashboard port
PORT=3000
